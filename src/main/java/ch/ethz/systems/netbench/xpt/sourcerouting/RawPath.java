package ch.ethz.systems.netbench.xpt.sourcerouting;

import edu.asu.emit.algorithm.graph.Path;

/**
 * this class simulates a path that was suggested but not approved yet by the router controller.
 * @author Yinnon
 *
 */
public class RawPath {
	
}
